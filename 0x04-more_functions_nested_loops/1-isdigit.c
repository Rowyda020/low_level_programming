#include "main.h"

/**
 * _isdigit - a function that checks for digits
 * @c: input
 * Return: 1 if c is digit, 0 otherwise
 */
int _isdigit(int c)
{
	if ((c >= 0) && (c <= 1))
		return (1);
	else
		return (0);
}
